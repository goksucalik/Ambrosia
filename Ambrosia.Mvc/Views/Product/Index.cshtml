@model Ambrosia.Entities.Dtos.ProductListDto
@{
    Layout = "_ProductLayout";
}
<!-- Banner -->
<section id="banner">
    <div class="inner">
        <div class="logo"><img src="~/BlogHome/images/ambrosia-logo.png" /></div>
    </div>
</section>
<section id="four" class="wrapper alt style1">
    <div class="inner">
        <h2 class="major">Menü</h2>
        <p>Ambrosia’nın menüsü, sağlıklı yaşam tarzını benimseyenler için özenle hazırlanmış seçenekler sunar. Her bir yemeğimiz ve içeceğimiz, doğanın en saf ve besleyici malzemeleri ile hazırlanır. Siz de bu eşsiz lezzetleri keşfetmek için Ambrosia’yı ziyaret edin ve sağlıklı yaşam yolculuğunuzda bize katılın!</p>
  
        <section class="features menu" id="productList">
            @foreach (var product in Model.Products)
            {
                <article>
                    <a asp-controller="Product" asp-action="Detail" asp-route-productId="@product.Id" class="image"><img src="@product.Thumbnail" alt="" /></a>
                    <h6 class="major">@product.Category.Name</h6>
                    <h3 class="major">@product.Name</h3>
                    <p>@product.Content</p>
                    <div class="price">
                        <strong class="new-price">@product.Price</strong>
                    </div>
                    <button href="#" class="btn">Sepete Ekle</button>
                </article>
            }

        </section>
        <ul class="pagination">
            @{
                @if (Model.ShowPrevious)
                {
                    @if (Model.CategoryId != null)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-route-currentPage="@(Model.CurrentPage-1)" asp-route-isAscending="@Model.IsAscending" asp-route-pageSize="@Model.PageSize" asp-route-categoryId="@Model.CategoryId.Value">Önceki</a>
                        </li>
                    }
                    else
                    {
                        <li class="page-item">
                            <a class="page-link" asp-route-currentPage="@(Model.CurrentPage-1)" asp-route-isAscending="@Model.IsAscending" asp-route-pageSize="@Model.PageSize">Önceki</a>
                        </li>
                    }

                }
                @for (var i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        @if (Model.CategoryId != null)
                        {
                            <a class="page-link" asp-route-currentPage="@i" asp-route-categoryId="@Model.CategoryId.Value" asp-route-isAscending="@Model.IsAscending" asp-route-pageSize="@Model.PageSize">@i</a>
                        }
                        else
                        {

                            <a class="page-link" asp-route-currentPage="@i" asp-route-isAscending="@Model.IsAscending" asp-route-pageSize="@Model.PageSize">@i</a>
                        }
                    </li>
                }
                @if (Model.ShowNext)
                {
                    @if (Model.CategoryId != null)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-route-currentPage="@(Model.CurrentPage+1)" asp-route-isAscending="@Model.IsAscending" asp-route-pageSize="@Model.PageSize" asp-route-categoryId="@Model.CategoryId.Value">Sonraki</a>
                        </li>
                    }
                    else
                    {
                        <li class="page-item">
                            <a class="page-link" asp-route-currentPage="@(Model.CurrentPage+1)" asp-route-isAscending="@Model.IsAscending" asp-route-pageSize="@Model.PageSize">Sonraki</a>
                        </li>
                    }

                }
            }
        </ul>

    </div>
</section>
<section id="footer">
    <div class="inner">
        <h2 class="major">Bizimle İletişime Geçin</h2>
        <p>Öneri, geri bildirim veya menümüze eklenmesini istediğiniz bir ürün hakkında bizimle iletişime geçmekten çekinmeyin. Ambrosia ailesi olarak, siz değerli müşterilerimizin görüşlerine büyük önem veriyoruz.</p>
        <form asp-controller="Home" asp-action="Contact" method="post">
            <div class="fields">
                <div class="field">
                    <label for="name">İsminiz</label>
                    <input type="text" name="name" id="name" />
                </div>
                <div class="field">
                    <label for="email">E-Posta adresiniz</label>
                    <input type="email" name="email" id="email" />
                </div>
                <div class="field">
                    <label for="message">Mesajınız...</label>
                    <textarea name="message" id="message" rows="2"></textarea>
                </div>
            </div>
            <ul class="actions">
                <li><input type="submit" value="Mesaj Gönder" /></li>
            </ul>

        </form>

        <ul class="contact">
            <li class="icon solid fa-home">
                Ambrosia Restaurant<br />
                Lezzet Caddesi 1400. Sokak<br />
                Ankara, Türkiye 06000
            </li>
            <li class="icon solid fa-phone">(000) 000-0000</li>
            <li class="icon brands fa-twitter"><a href="https://twitter.com/AmbrosiaDünyası" target="_blank">twitter.com/AmbrosiaDünyası</a></li>
            <li class="icon brands fa-facebook-f"><a href="https://facebook.com/AmbrosiaDunyasi" target="_blank">facebook.com/AmbrosiaDunyasi</a></li>
            <li class="icon brands fa-instagram"><a href="https://instagram.com/AmbrosiaDunyasi" target="_blank">instagram.com/AmbrosiaDunyasi</a></li>
        </ul>
        <ul class="copyright">
            <li>Tüm Hakları Saklıdır &copy; Ambrosia 2024</li>
        </ul>
    </div>
</section>
@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var sortOrder = new URLSearchParams(window.location.search).get('sortOrder');
            if (sortOrder) {
                document.getElementById("sortOrder").value = sortOrder;
            } else {
                document.getElementById("sortOrder").value = "default";
            }
        });

        function sortProducts() {
            var sortOrder = document.getElementById("sortOrder").value;
            window.location.href = '@Url.Action("Index", "Product")' + '?sortOrder=' + sortOrder;
        }
    </script>
}
